{
  "name": "codex-material-themes",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "node scripts/clean-dist.mjs && vite build && node scripts/create-site-worker.mjs",
    "start": "vite preview",
    "test": "npm run build"
  },
  "devDependencies": {
    "vite": "8.0.13"
  },
  "type": "module"
}
